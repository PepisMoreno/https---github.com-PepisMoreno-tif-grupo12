Clone repo nuevo git clone http://..
Cree una rama desde pag web de git

Agregué el código de ejemplo de MArcela C que estaba en el PDF

Chequeo que tenga instalado python, pip y virtualenv
--> virtualenv --version
OK

$ flask --version
Python 3.12.0
Flask 3.0.0
Werkzeug 3.0.1


Instalé CORS
marshmallow Y sqlalchemy

__ en app.py     sql= "INSERT INTO plantas (nombre_comun, nombre_científico, foto) VALUES (%s, %s, %s)";
le cambie las S por s

Correr en terminal python app.py

PRUEBO DE MIL MANERAS CONECTAR CON MYSQL. no sé
PRuebo con el video de MArcela Cerda: ok, no me tira errores
pero no corre el bd

ERROR:
"IntegrityError
Exception raised when the relational integrity of 
the database is affected, e.g. a foreign key check fails.
This error is a DBAPI Error and originates from the 
database driver (DBAPI), not SQLAlchemy itself."
